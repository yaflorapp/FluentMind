import { AppLayout } from '@/components/layout/app-layout';
import { LessonView } from '@/components/classroom/lesson-view';

export default function ClassroomPage() {
  return (
    <AppLayout>
      <LessonView />
    </AppLayout>
  );
}
